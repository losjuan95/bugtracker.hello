@model IEnumerable<bugtracker.Models.TicketNotification>

@{
    ViewBag.Title = "Index";
    
}

<!--main content start-->
<section id="main-content">
    <section class="wrapper site-min-height">
        <h3><i class="fa fa-angle-right"></i> Notifications</h3>
        <div class="row mt">
            <div class="col-lg-12">
                <table class="table">
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.Recipient.FirstName)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Ticket.Title)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.NoticationBody)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Created)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Read)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Confirmed)
                        </th>
                        <th></th>
                    </tr>

                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Recipient.FirstName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Ticket.Title)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.NoticationBody)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Created)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Read)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Confirmed)
                            </td>
                            <td>
                                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                            </td>
                        </tr>
                    }

                </table>
            </div>
        </div>
    </section>
    <!-- /wrapper -->
</section>
<!-- /MAIN CONTENT -->



